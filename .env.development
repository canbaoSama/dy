# 不设 VITE_API_BASE 时，axios 使用相对路径 /api/v1，由 Vite 代理到后端。
# 当前本机 8000 被旧进程占用且缺少 /candidates/translate，开发默认改为 8010。
VITE_API_TARGET=http://127.0.0.1:8010
# 仅在需要「浏览器直连后端、不走代理」时取消注释并改成实际端口，例如：
# VITE_API_BASE=http://127.0.0.1:8010/api/v1
